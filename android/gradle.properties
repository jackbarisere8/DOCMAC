org.gradle.java.home=C:/Program Files/Android/Android Studio1/jbr
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djava.security.manager=allow --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# Flutter's Gradle plugin currently uses the legacy Android DSL.
# Keep it enabled while building with AGP 9.
android.newDsl=false
# Temporarily disable AGP 9 built-in Kotlin compatibility.
# This bypasses the older FlutterFire plugin behavior until the plugin is upgraded.
android.builtInKotlin=false
